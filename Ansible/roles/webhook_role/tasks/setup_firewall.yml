# tasks/setup_firewall.yml
- name: Open port "{{ webhook_app_port }}" in firewall
  ansible.builtin.firewalld:
    port: "{{ webhook_app_port }}/tcp"
    state: enabled
    permanent: yes
    immediate: yes
  ignore_errors: yes
